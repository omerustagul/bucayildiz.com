The homepage hero CTA — "ücretsiz denemelere katıl" card. Navy gradient, gold edge + star watermark, gold accent button.

```jsx
<TrialBanner href="/basvuru" />
<TrialBanner title="Yaz Okulu Başladı" ctaLabel="Kayıt Ol" />
```

All copy is overridable. Renders a `Button variant="accent"` internally, so it's the one place gold is used boldly.
