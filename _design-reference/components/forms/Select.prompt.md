Native select with brand styling.

```jsx
<Select label="Yaş Grubu" placeholder="Seçiniz"
  options={['U-15','U-16','U-17','U-18','A Takım']} />
```

`options` accepts strings or `{value,label}`. Same label/hint/required pattern as `Input`.
